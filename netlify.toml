[build]
  command = "node scripts/validate-env.mjs && npm run lint && npm run build"
  publish = ".next"
  node_bundler = "esbuild"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
